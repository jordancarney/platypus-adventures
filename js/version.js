// Single source of truth for the build version.
// Bump this on every deploy: live sessions compare it against the copy of this file on
// the server (fetched with caching disabled) and refresh themselves when it changes.
export const VERSION = '1.7.1';
